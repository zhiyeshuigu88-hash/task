{
  // テストは Node の test runner + 型ストリッピングで直接実行する。
  // そのため相対 import に .ts 拡張子が必要で、node の型定義も要る。
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "types": ["node"],
    "jsx": "react-jsx"
  },
  "include": ["./**/*.ts"]
}
